<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f5f7fb;
        }

        .sidebar {
            min-height: 100vh;
            background-color: #343a40;
        }

        .sidebar-menu a {
            color: rgba(255, 255, 255, 0.8);
            transition: all 0.3s;
        }

        .sidebar-menu a:hover,
        .sidebar-menu a.active {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .stat-card {
            height: 100%;
        }

        .badge-success-light {
            background-color: rgba(25, 135, 84, 0.1);
            color: #198754;
        }

        .badge-danger-light {
            background-color: rgba(220, 53, 69, 0.1);
            color: #dc3545;
        }

        .badge-warning-light {
            background-color: rgba(255, 193, 7, 0.1);
            color: #ffc107;
        }

        .badge-info-light {
            background-color: rgba(13, 202, 240, 0.1);
            color: #0dcaf0;
        }
    </style>
    <style>
        html {
            scroll-behavior: smooth;
        }
    </style>
    
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <%- include('partials/sidebar') %>
    
            <!-- Main content -->
            <div class="col-md-9 col-lg-10 ms-auto px-md-4">
                <!-- Account Info Header -->
                <%- include('partials/header') %>
    
                <!-- Stats overview -->
                <section id="stats">
                    <%- include('partials/stats') %>
                </section>
    
                <!-- Classroom list -->
                <section id="classroom">
                    <%- include('dashboard/class') %>
                </section>
    
                <!-- Account list -->
                <section id="account">
                    <%- include('dashboard/account', { accounts: accounts }) %>
                </section>
                <!-- Booking list -->
                <section id="booking">
                    <%- include('dashboard/booking', { bookings: bookings }) %>
                </section>
    
                <!-- Feedback -->
                <section id="feedback">
                    <%- include('dashboard/feedback', { feedback: feedback }) %>
                </section>
                <!-- Support -->
                <section id="support">
                    <%- include('dashboard/support', { supports: supports }) %>
                </section>
            </div>
        </div>
    </div>
    
    <!-- Bootstrap JS bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
        });
    </script>
</body>

</html>